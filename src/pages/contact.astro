---
import Base from '../layouts/Base.astro';
import { SITE, whatsappLink, mailtoLink } from '../site';
import page from '../content/pages/contact.json';
---
<Base title={page.seo.title} description={page.seo.description}>
  <section class="page-hero">
    <div class="container">
      <span class="eyebrow">{page.hero.eyebrow}</span>
      <h1>{page.hero.heading}</h1>
      <p>{page.hero.intro}</p>
    </div>
  </section>

  <section class="section--white" style="padding-block: var(--section-y);">
    <div class="container split" style="align-items: start;">
      <div class="form-col">
        <form
          class="card card--pad"
          name="contact"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          action="/contact/?success=true"
        >
          <input type="hidden" name="form-name" value="contact" />
          <p hidden><label>Don't fill this in: <input name="bot-field" /></label></p>
          <h2>{page.formHeading}</h2>
          <div class="field">
            <label for="c-name">Name</label>
            <input id="c-name" type="text" name="name" autocomplete="name" required />
          </div>
          <div class="field">
            <label for="c-email">Email</label>
            <input id="c-email" type="email" name="email" autocomplete="email" required />
          </div>
          <div class="field">
            <label for="c-phone">Phone (optional)</label>
            <input id="c-phone" type="tel" name="phone" autocomplete="tel" />
          </div>
          <div class="field">
            <label for="c-msg">Message</label>
            <textarea id="c-msg" name="message" rows="5" required></textarea>
          </div>
          <button class="btn btn--primary" type="submit" data-cta="contact-send">Send Message</button>
          <p class="form-note">We use your details only to reply to you. See our <a href="/policies/" data-cta="contact-privacy">privacy policy</a>.</p>
        </form>
        <div id="contact-success" class="card card--pad form-success" role="status" hidden>
          <h2>{page.success.heading}</h2>
          <p>{page.success.text} <a href={whatsappLink()} rel="noopener" data-cta="contact-success-message">Message us{SITE.whatsappNumber ? ' on WhatsApp' : ' on Instagram'}</a>.</p>
        </div>
      </div>

      <div>
        <h2>{page.directHeading}</h2>
        <div class="line">
          <h3>Instant message</h3>
          <p><a href={whatsappLink()} rel="noopener" data-cta="contact-direct-message">Message KLUB</a> — {page.messageNote}</p>
        </div>
        <div class="line">
          <h3>Email</h3>
          <p><a href={mailtoLink()} data-cta="contact-direct-email">{SITE.email}</a></p>
        </div>
        <div class="line">
          <h3>Instagram</h3>
          <p><a href={SITE.instagram} rel="noopener" data-cta="contact-direct-ig">{SITE.instagramHandle}</a> — {page.instagramNote}</p>
        </div>
        <div class="line">
          <h3>Visit</h3>
          <p>
            {SITE.streetAddress ? <>{SITE.streetAddress}<br /></> : null}
            {SITE.addressLocality}, {SITE.addressCountry}
            {!SITE.streetAddress && <><br /><span class="note">{SITE.addressNote}</span></>}
          </p>
        </div>
      </div>
    </div>
  </section>

  <script is:inline>
    if (new URLSearchParams(location.search).get('success') === 'true') {
      var f = document.querySelector('form[name="contact"]');
      var ok = document.getElementById('contact-success');
      if (f && ok) {
        f.hidden = true;
        ok.hidden = false;
        ok.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    }
  </script>
</Base>

<style>
  .line { margin-bottom: 26px; }
  .line h3 { font-size: 19px; margin-bottom: 4px; }
  .line p { color: var(--ink-soft); }
</style>
